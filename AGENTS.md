# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Design

Any UI work — screens, components, styling, copy — follows
`.claude/skills/fitfork-design/SKILL.md`. Visual values come from
`src/theme/tokens.ts`; never inline hex codes, font sizes, or font weights.
